# 项目文件清理报告

| 项 | 内容 |
|---|---|
| 日期 | 2026-08-15 |
| 版本 | v1.3.1 |
| 范围 | 仓库根目录、`ls-aegis-server`、`ls-aegis-web`、`docker`、`reports` |
| 方式 | 只读审计（2026-08-15）→ **A–G 已落地**（同日） |
| 结论 | 临时垃圾与密钥探针已较干净；主要问题是 **已入库的前端构建产物**、**未使用前端组件/大数据文件**、**演示任务与命名债务**；行动计划 A–G 已执行（MD5 字段命名债务留待后续兼容别名 PR） |

---

## 1. 总体结论

仓库整体结构清晰（前后端分离 + Docker + 报告目录），符合既有「报告规范 / SQL 规范 / Git 规范」的大方向：

- 无 `*.tmp` / `*.bak` / 根目录 `*.log` / `gm-verify-*.log` / `*-tmp.json` 等探针残留
- `target/`、`node_modules/`、`docker/data/`、`.idea/`、`.vscode/`、密钥路径均已正确忽略
- 正式报告集中在 `reports/**/*.md`；部署 SQL 仅 `ls-aegis-server/docs/sql/init.sql`
- 根目录精简：仅有 `.gitignore`、`VERSION` 与业务目录

审计时发现的主要缺口（落地后状态）：

1. ~~将 Vite 构建产物提交进 Git~~ → **已取消跟踪**；Compose 用 `docker/nginx/Dockerfile` 多阶段构建打入镜像；`scripts/sync-web-to-nginx.*` 仅作可选覆盖
2. ~~无引用组件与 `world.json`~~ → **已删除**
3. ~~演示环境清理任务默认注册~~ → **已加 `@Profile("demo")`**
4. ~~报告文案仍引用 `ls-demo`~~ → **已校正为 `ls-aegis-boot`**
5. ~~缺少仓库根 README~~ → **已补齐**
6. ~~遗留：分片上传 `fileMd5` / `MD5_TO_UPLOAD_*` 命名~~ → **已落地并去掉兼容别名**；库列 `file_digest`；普通上传亦统一 SM3

---

## 2. 仓库健康度速览（落地后）

| 指标 | 落地前 | 落地后 | 说明 |
|---|---|---|---|
| `docker/nginx/web` 跟踪数 | 645 | **2** | `.gitkeep` + `README.md` |
| `world.json` | ~2.7 MB | **已删除** | 仅保留 `china.json` |
| `import *` Java 文件 | 36 | **0** | 已全部改为显式 import |
| 无用 Gi*/SplitPanel/TextCopy | 8 组 | **已删除** | `components.d.ts` 已同步 |
| 根目录 README | 无 | **有** | 含 Docker 同步说明 |
| 本地 mock | 默认开 | **默认关** | `VITE_LOCAL_MOCK=true` 才启用 |

---

## 3. 已符合规范（无需清理）

| 类别 | 路径 / 状态 | 结论 |
|---|---|---|
| 报告落盘 | `reports/**/*.md` | 合规 |
| 部署 SQL | `docs/sql/init.sql` + `.gitkeep` | 合规 |
| 构建缓存 | `**/target/`、`node_modules/` | 已 ignore，勿提交 |
| 运行时数据 | `docker/data/`、`data/gm/`、`backups/` | 已 ignore |
| IDE 配置 | `.idea/`、`.vscode/` | 未入库 |
| 模块说明 | server/web/crypto/open/schedule README | 合理保留 |

---

## 4. 问题清单（按优先级）

### P0 — 建议尽快处理（体积 / 规范 / 风险）

#### P0-1 跟踪了前端构建产物

| 项 | 内容 |
|---|---|
| 路径 | `docker/nginx/web/`（尤其 `static/`） |
| 问题 | 哈希化 JS/CSS 随每次前端构建变化；与源码双重维护；违反「不提交 dist」惯例；`docker-compose` 将该目录挂载为 Nginx 站点根 |
| 建议 | ① `.gitignore` 增加 `docker/nginx/web/**`（保留 `.gitkeep` 或 README 说明）② `git rm -r --cached docker/nginx/web` ③ 文档化：`pnpm build` → 同步到 `docker/nginx/web/` 再 `compose up` ④ CI/脚本固化同步步骤 |
| 风险 | 裸 clone 后若不构建，本地 Docker 前端为空；需同步更新部署说明 |
| 置信度 | 高 |

#### P0-2 演示环境任务可能误伤数据

| 项 | 内容 |
|---|---|
| 路径 | `ls-aegis-server/ls-aegis-boot/.../job/DemoEnvironmentJob.java` |
| 问题 | ContiNew 遗留「演示环境清理」任务；`@Component` + `@JobExecutor` 始终注册；内部会清理字典/用户/菜单等演示数据；含 `import *` |
| 建议 | 删除，或加 `@Profile("demo")` / 配置开关，默认不加载；生产禁止启用 |
| 置信度 | 高 |

#### P0-3 未使用且体积巨大的世界地图数据

| 项 | 内容 |
|---|---|
| 路径 | `ls-aegis-web/src/components/Chart/world.json` |
| 问题 | `Chart/index.vue` 无条件 `registerMap('world', …)`；仪表盘 `Geo.vue` 中「世界」单选为 **disabled / 暂未开放**，业务仅用 `china` |
| 建议 | 删除 `world.json` 及注册逻辑；若未来开放再按需动态加载 |
| 预估收益 | 源码约 -2.7 MB，构建包同步减小 |
| 置信度 | 高 |

---

### P1 — 建议本迭代清理（死代码 / 债务）

#### P1-1 无业务引用的前端组件

以下组件在 `src/views` 及其他业务代码中 **无引用**（仅组件自身与自动生成的 `components.d.ts`）：

| 组件目录 | 建议 |
|---|---|
| `src/components/GiSpace/` | 删除或迁入可选组件库文档 |
| `src/components/GiIframe/` | 同上 |
| `src/components/GiEditTable/` | 同上 |
| `src/components/GiIconBox/` | 同上 |
| `src/components/GiTag/` | 同上 |
| `src/components/GiSplitPane/` | 同上 |
| `src/components/TextCopy/` | 同上 |
| `src/components/SplitPanel/` | 同上 |

删除后需重新生成 / 清理 `src/types/components.d.ts`。  
置信度：中高（unplugin 自动注册，静态检索无业务标签；删除前建议再跑一遍构建与关键页面冒烟）。

#### P1-2 已废弃且无调用的加密导出

| 项 | 内容 |
|---|---|
| 路径 | `ls-aegis-web/src/utils/encrypt.ts` |
| 问题 | `encryptByMd5`、`encryptByRsa` 标注 `@deprecated`，全仓无外部调用；业务仅用 `encryptTransport` / `encryptByCaptcha` / `loadGmPublicKey` |
| 建议 | 删除上述两个函数；AES 回退默认密钥字面量 `XwKsGlMcdPMEhR1B` 改为强制外部配置（国密关闭时） |
| 置信度 | 高 |

#### P1-3 类名笔误

| 项 | 内容 |
|---|---|
| 路径 | `.../RedisMultipartUploadDaoDaoImpl.java` |
| 问题 | `DaoDao` 重复 |
| 建议 | 重命名为 `RedisMultipartUploadDaoImpl`（同步 Spring 扫描 / 引用） |
| 置信度 | 高 |

#### P1-4 过时 npm 类型包

| 项 | 内容 |
|---|---|
| 路径 | `ls-aegis-web/package.json` → `@types/query-string` |
| 问题 | `query-string` 已自带类型，该 stub 已 deprecated |
| 建议 | 从 `devDependencies` 移除并更新 lockfile |
| 置信度 | 高 |

#### P1-5 依赖分类不当

| 依赖 | 现状 | 建议 |
|---|---|---|
| `vite-plugin-vue-devtools` | 在 `dependencies` | 移至 `devDependencies` |
| `lint-staged` | 在 `dependencies` | 移至 `devDependencies` |

置信度：中。

---

### P2 — 规范与文档整理

#### P2-1 报告中的过时路径 `ls-demo`

**已处理（2026-08-15）**：国密/安全相关报告路径已统一为 `ls-aegis-boot`，并注明历史模块名；两份安全总报告已互为补充标注。

#### P2-2 缺少根 README

根目录无 `README.md`。建议增加：目录结构、版本号来源（`VERSION`）、前后端启动、Docker 构建与前端产物同步、国密开关要点、报告目录入口。

#### P2-3 `.gitignore` 缺口

当前未忽略 `docker/nginx/web/**`。与 P0-1 一并处理。  
~~可选：大体积截图目录 `ls-aegis-web/.image/screenshot/`~~ → **已取消跟踪并 gitignore**（保留 `.image/README.md` 与 `qrcode.jpg`）。

#### P2-4 Java `import *`（36 个文件）

违反项目「禁止 `import *`」。建议分批改为显式 import；可与功能改动一并改，避免无意义大 diff。

#### P2-5 命名债务（MD5 字段名）

**已处理（2026-08-15）**：

| 层 | 新约定 | 状态 |
|---|---|---|
| API | 仅 `fileDigest` | **已删除** `fileMd5` / 响应 `sha256` 别名 |
| Redis | 仅 `multipart:digest_to_upload:` | **已删除** 旧前缀双写 |
| 普通上传 | 上传前算 SM3 写入 `file_digest` | **已去掉** x-file-storage SHA256 |
| 存量迁移 | 重读存储原文重算 SM3 | **已落地并已在本机 Docker 执行**（`scanned=0`，库中暂无文件；列已 `sha256`→`file_digest`） |
| 演示截图 | `ls-aegis-web/.image/screenshot/` | **已取消跟踪并 gitignore** |

#### P2-6 Mock 默认开启

`ls-aegis-web/config/plugins/mock.ts` 中 `localEnabled: true`，且 `src/mock/_data/area.ts` 约 336 KB。建议改为环境变量控制，避免本地误走 mock。

#### P2-7 未完成 TODO

`ls-aegis-web/src/hooks/modules/useChart.ts`：`// TODO 图表主题` — 实现或删除占位。

#### P2-8 前后端重复 china 地图

| 路径 | 大小约 |
|---|---|
| `ls-aegis-web/.../Chart/china.json` | 2.1 MB |
| `ls-aegis-server/.../resources/china.json` | 0.35 MB |

用途可能不同（前端 ECharts vs 后端接口）。短期可保留；长期明确单一数据源。

---

## 5. 非问题（勿误删）

| 项 | 说明 |
|---|---|
| Spring `@Component` / Controller 单文件名命中 | 由容器注入，不能用「引用次数=1」判死代码 |
| `AESUtil` 覆盖包 `com.anji.captcha.util` | 国密验证码有意覆盖，保留 |
| Schedule `DefaultController` 等桩 | 关闭任务时的条件装配，保留 |
| ContiNew 版权头 | 许可证溯源，不作为垃圾清理 |

---

## 6. 建议执行顺序（清理行动计划）

| 阶段 | 动作 | 预估收益 | 状态（2026-08-15） |
|---|---|---|---|
| A | 改 `.gitignore` + 取消跟踪 `docker/nginx/web` + 同步脚本/文档 | 仓库 -12 MB+，规范对齐 | **已完成**；开箱体验已加强为 **Nginx 多阶段镜像**（`docker/nginx/Dockerfile`，裸 clone `compose up --build` 即可） |
| B | Profile 隔离 `DemoEnvironmentJob` | 降低误清数据风险 | **已完成**（`@Profile("demo")`，显式 import） |
| C | 移除 `world.json` 及注册 | 源码/包体减小 | **已完成** |
| D | 删除无引用 Gi*/SplitPanel/TextCopy；删废弃 encrypt；去 `@types/query-string` | 减负、降噪音 | **已完成** |
| E | 重命名 `RedisMultipartUploadDaoDaoImpl` → `RedisMultipartUploadDaoImpl` | 可读性 | **已完成** |
| F | 修正 reports 内 `ls-demo`；补根 README；标注重复安全报告 | 文档一致 | **已完成** |
| G | `import *`、mock 开关、依赖分类；指纹命名 | 长期规范 | **已完成**（含 `fileDigest` API/Redis 双写；库列 `sha256`→`file_digest`） |

---

## 7. 本报告范围说明

- **已完成（审计）**：全仓结构扫描、大文件/构建产物统计、死代码与规范违背项核对、清理优先级与行动计划。
- **已完成（落地 A–G）**：见上表；`docker/nginx/web` 构建产物不入库。
- **开箱部署**：`web` 服务改为多阶段构建，静态资源打入镜像，不再依赖预同步目录。
- **指纹命名**：仅 `fileDigest` + Redis `digest_to_upload`；库列 `file_digest`；普通上传与分片均 SM3；演示截图已移出版本库。
- **存量 SHA256→SM3**：无法直接换算，须重读存储原文（流式 SM3）；默认关闭 `file.digest.migrate-to-sm3` / `FILE_DIGEST_MIGRATE_TO_SM3`，运维窗口开启一次或调用 `POST /system/file/migrate-digest-sm3`（权限 `system:file:update`），完成后关闭开关。本机 Docker 已跑过一轮：`scanned=0, updated=0`（无存量文件）；有数据的环境同法执行即可。

---

## 8. 附录：关键命令备忘（执行阶段用）

```bash
# 统计 nginx 构建产物跟踪数（应为 2：.gitkeep + README）
git ls-files docker/nginx/web

# 推荐：Compose 内构建前端并启动
cd docker && docker compose up -d --build

# 可选：本机 dist → docker/nginx/web（仅当自行挂载覆盖时）
./scripts/sync-web-to-nginx.sh          # Linux/macOS
.\scripts\sync-web-to-nginx.ps1         # Windows
```

（`vite` 输出目录为 `ls-aegis-web/dist`。）
