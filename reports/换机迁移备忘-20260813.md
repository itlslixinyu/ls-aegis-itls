# 换机迁移备忘-20260813

正式迁移包在仓库外目录（已 gitignore）：

`backups/`（本机路径示例：`D:\LS・Aegis\backups\`）

内含：Git bundle（v1.2.1）、MySQL dump、`docker.env` 副本、国密 `keys.properties`、以及《换机迁移说明.md》。

**请整包加密拷走；勿提交仓库。** 新电脑按该说明恢复。当前代码版本：**v1.2.2**（在 v1.2.1 业务提交上增加换机备忘）。
