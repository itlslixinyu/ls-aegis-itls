# 业务扩展模块示例（开放能力）

模块：`ls-aegis-biz-open`，包名：`com.ls.aegis.biz.open`。

仅依赖 `ls-aegis-starter`。标准分层：

- `controller` / `service` / `service.impl` / `mapper` / `entity`（po、dto、vo、query）
- `resources/mapper`、`application.yml`
