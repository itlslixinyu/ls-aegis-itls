---
description: 全局强制：回复与代码注释使用简体中文
alwaysApply: true
---

# 简体中文输出
## 语言&输出
1. 默认输出中文注释、中文错误提示、中文接口文档；代码关键字保留英文。
2. 生成日志、异常信息、业务提示全部中文，不要输出大段英文说明。

## 依赖&镜像
1. Maven依赖优先使用国内中央库可用的稳定版本，避免选用国外冷门、国内难下载的依赖包。
2. 不生成国外私有源、github raw地址、海外镜像地址；不引入国外闭源SDK。
3. 优先选用国产组件：国密工具、hutool、Sa‑Token，减少对海外组件强依赖。

## 网络&部署
1. Docker、Nginx配置适配国内服务器环境，不生成外网访问才可用的配置。
2. 输出yaml、docker配置禁止写外网域名，优先留占位符。
3. 避免生成需要访问外网才能运行的代码片段。

## 数据库&字符集
1. MySQL默认字符集 utf8mb4，排序规则 utf8mb4_general_ci，适配中文存储，处理中文乱码。
2. 时间字段使用datetime，禁止使用timestamp，兼容国内时区东八区。

## 安全合规（国内项目）
1. 密码、密钥禁止硬编码写在代码、yml，统一放到环境变量。
2. 生成SQL不要带MySQL的root用户，使用业务账号占位符。
3. 敏感数据做脱敏示例：手机号、身份证、真实姓名日志脱敏。

## 代码生成约束
1. 不生成import * 批量导入；import要具体类。
2. 不生成测试main方法，业务代码只输出业务逻辑。
3. 生成代码尽量贴合JDK21，SpringBoot3生态，适配国产化JDK。
4. 不要输出idea、vscode本地配置文件内容(.idea、.vscode)。

## 项目约束
1. 严格遵守LS·Aegis目录结构，业务代码放在 com.ls.biz.xxx，不污染底层ls‑*模块。
2. 禁止PO直接作为接口入参出参，使用DTO/VO隔离。