---
description: 项目通用规范：简体中文/国产化约定 + Git 提交与版本号
alwaysApply: true
---

# 项目与 Git 规范

## 一、语言与工程约定

### 语言&输出
1. 默认输出中文注释、中文错误提示、中文接口文档；代码关键字保留英文。
2. 生成日志、异常信息、业务提示全部中文，不要输出大段英文说明。

### 依赖&镜像
1. Maven 依赖优先使用国内中央库可用的稳定版本，避免国外冷门、难下载的包。
2. 不生成国外私有源、github raw、海外镜像；不引入国外闭源 SDK。
3. 优先国产组件：国密、hutool、Sa-Token。

### 网络&部署
1. Docker、Nginx 适配国内环境，不生成必须外网才可用的配置。
2. yaml/docker 配置禁止写死外网域名，优先占位符。
3. 避免生成必须访问外网才能运行的代码片段。

### 数据库&字符集
1. MySQL：`utf8mb4` + `utf8mb4_general_ci`；时间用 `datetime`，禁用 `timestamp`（东八区）。
2. SQL 中文与部署同步详见规则「SQL规范」。

### 安全与代码
1. 密码、密钥禁止硬编码，放环境变量；SQL 不用 root，用业务账号占位符。
2. 敏感数据日志脱敏（手机号、身份证、姓名）。
3. 禁止 `import *`；不写测试 main；贴合 JDK21 / Spring Boot 3。
4. 不输出 `.idea`、`.vscode` 本地配置内容。
5. 业务代码在 `com.ls.aegis.biz.xxx`，不污染 `ls-aegis-*` 基础模块；接口用 DTO/VO，禁止 PO 直接出入参。

## 二、Git 提交与版本号

仅在用户明确要求「提交 / commit / 帮我提交」时 commit。提交前按仓库规范检查 status / diff / log。

### 版本文件（以根目录为准）

1. 根目录 `VERSION`（如 `1.0.0`）为唯一权威版本；不存在时从 `1.0.0` 起。
2. 同步：`ls-aegis-web/package.json` 的 `version`
3. 同步：`ls-aegis-server/pom.xml` 的 `<revision>`
4. 其它明显工程版本字段一并对齐

### 升版规则（版本号 `X.Y.Z`）

默认按三位版本号升版；升某一位时，其后位数归零。

| 用户意图 | 升哪一位 | 示例 |
|---------|---------|------|
| 未说明 / 普通提交 / fix / 文档 | **末位 Z +1**（补丁） | `1.0.0` → `1.0.1` |
| 小版本 / 新功能 / feature / 增强 / minor | **中间位 Y +1**，Z 归零 | `1.0.1` → `1.1.0` |
| 大版本 / 破坏性变更 / major / 不兼容 | **首位 X +1**，Y、Z 归零 | `1.1.0` → `2.0.0` |
| 明确指定版本号 | 用用户给定版本 | — |
| 只提交不升版本 | 不改版本、不打 tag；仍可 push 分支 | — |

### 提交、Tag、推送

1. 说明格式：`release: vX.Y.Z <一两句中文说明为什么>`（或 `chore(release): vX.Y.Z …`）
2. 升版时打 tag：`vX.Y.Z`（与提交同版本；勿 force 改已有 tag）
3. 默认自动 push 当前分支；有新 tag 则 `git push origin vX.Y.Z`
4. 用户说「不要推送 / 先别 push」→ 仅本地 commit/tag
5. 禁止 `--force` 推 main/master，除非用户明确要求
6. 不要提交密钥、`docker/data`、`node_modules`、`target`
7. push 失败时：完成本地 commit/tag，告知用户自行 `git push`
