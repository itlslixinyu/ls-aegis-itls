---
description: 用户说「提交」时自动升小版本号（补丁位）并写入发版提交
alwaysApply: true
---

# 提交即升小版本号

当用户说「提交」「commit」「帮我提交」等（未额外声明大版本/次版本）时：

1. **自动升补丁版本**（语义化第三位）：如 `1.0.0` → `1.0.1` → `1.0.2`
2. **同步改版本号文件**（若存在）：
   - `ls-aegis-platform/pom.xml` 中 `<revision>`
   - `ls-aegis-web/package.json` 中 `"version"`
   - 其它明显的工程版本字段一并对齐
3. **提交说明格式**（沿用仓库风格）：
   - `release: vX.Y.Z <一两句中文说明本次改动为什么>`
4. **打 git tag**：`vX.Y.Z`（与提交同版本；勿 force 改已有 tag）
5. **自动 push**：提交并打 tag 后，推送当前分支与对应 tag 到 remote（`git push` + `git push origin vX.Y.Z`）。禁止 `--force` 推送到 main/master，除非用户明确要求。

例外：

- 用户明确说「大版本 / major」→ 升第一位，次、补丁归零
- 用户明确说「次版本 / minor」→ 升第二位，补丁归零
- 用户说「只提交不升版本」→ 普通 commit，不改版本号、不打 tag；仍自动 push 分支（无新 tag 则不推 tag）
- 用户说「不要推送 / 先别 push」→ 仅本地提交与打 tag，不 push
